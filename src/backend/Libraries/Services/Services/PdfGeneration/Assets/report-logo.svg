<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 64" width="300" height="64" role="img" aria-label="App Template">
  <title>App Template</title>
  <desc>
    Placeholder brand lockup embedded into the Services assembly and rendered in the header of every
    generated PDF report. Replace this file to rebrand your reports — it is backend-owned on purpose,
    so the backend never reaches into the frontend tree for a build input. Keep the colours explicit
    (no `currentColor`): the PDF renderer has no surrounding text colour to inherit from.
  </desc>
  <defs>
    <linearGradient id="reportLogoFill" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#3B82F6" />
      <stop offset="1" stop-color="#1D4ED8" />
    </linearGradient>
  </defs>

  <!-- Mark -->
  <rect width="64" height="64" rx="14" fill="url(#reportLogoFill)" />
  <path
    d="M19 45.5 L32 17 L45 45.5"
    fill="none"
    stroke="#ffffff"
    stroke-width="5.5"
    stroke-linecap="round"
    stroke-linejoin="round"
  />
  <path d="M25 36.5 H39" fill="none" stroke="#ffffff" stroke-width="5.5" stroke-linecap="round" />

  <!-- Wordmark -->
  <text
    x="82"
    y="41"
    font-family="Lexend, Inter, 'Segoe UI', system-ui, -apple-system, sans-serif"
    font-size="26"
    font-weight="650"
    letter-spacing="-0.4"
    fill="#0F172A"
  >App Template</text>
</svg>
